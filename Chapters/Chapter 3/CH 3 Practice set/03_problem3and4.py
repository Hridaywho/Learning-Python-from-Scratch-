name = "Harry is a good boy  and  "

print(name.find("  "))

#if double space is there then the output would be some number other than -1 
#if double space is not there then the output would be -1


name = "Harry is a good boy  and  "

print(name.replace("  "," ")) 

#this will replace the double space with single space
#Strings ae immutable which means you cannot change them by running functions on them
